; Group Properties
(ns groups.group-properties
  (:require [clojure.math.numeric-tower :as math]))

(defn order
  "Number of elements in the group"
  [G]
  (count G))

(defn is-abelian
  "Checks if group is abelian"
  [G]
  )
